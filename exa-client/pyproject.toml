[project]
name = "exa-client"
version = "0.1.0"
description = "Exa API client for web search queries"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "exa-py>=1.0",
    "python-dotenv>=1.0",
]

[project.scripts]
exa-client = "main:main"
